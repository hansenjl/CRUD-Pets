<h1>One Pet</h1>

<p> <%= @pet.name %> is a <%= @pet.species%> and is <%= @pet.age %> years old. </p>

<%= link_to "Edit Pet", edit_pet_path(@pet) %>
<%= link_to "Delete Pet", pet_path(@pet), method: :delete  %>

