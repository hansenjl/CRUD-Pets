<h1>Edit Pet!</h1>

<%= form_for(@pet) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>


  <%= f.label :species %>
  <%= f.select(:species, [ "Dog", "Cat", "Fish" ]) %>

  <%= f.label :age %>
  <%= f.number_field :age, min:0 %>

  <%= f.submit %>

<% end %>